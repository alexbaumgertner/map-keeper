# ODbL / licensing notes for Mapkeeper

## Status

TODO: Record whether Mapkeeper’s database (OSM snapshots + confirmed attributes + candidates)
constitutes a Derivative Database and/or Collective Database under ODbL, and what share-alike
obligations follow for public dumps.

See: OSM Collective Database Guideline.

## Working rules (constitution)

- Candidate data (Overture, website parse, etc.) stays in a separate layer until human confirmation.
- Per-attribute `source` is mandatory; `google` is never a valid source.
- No public dump until this document’s conclusion is ratified.

## Provisional stance (draft)

Until counsel/community review, treat Mapkeeper’s store of OSM snapshots plus confirmed attributes as potentially a **Collective Database** under ODbL when published as a dump; keep candidates segregated; prefer not shipping bulk extracts in MVP.
